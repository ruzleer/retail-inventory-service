info:
  name: product-id
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/product/1
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
