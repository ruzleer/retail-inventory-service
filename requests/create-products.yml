info:
  name: create-products
  type: http
  seq: 1

http:
  method: POST
  url: http://localhost:8080/create-products
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
