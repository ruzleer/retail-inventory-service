info:
  name: products
  type: http
  seq: 1

http:
  method: GET
  url: http://localhost:8080/products
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
​